<!-- Begin page heading -->
<h1 class="page-heading">Professeurs</h1>
<!-- End page heading -->

<!-- Begin breadcrumb -->
<ol class="breadcrumb default square rsaquo sm">
	<li><a href="/admin"><i class="fa fa-home"></i></a></li>
	<li class="active">Professeurs</li>
</ol>
<!-- End breadcrumb -->

<a class="btn btn-info btn-perspective" href="<%= new_admin_teacher_path %>"><i class="fa fa-plus"></i> Nouveau professeur</a>

<!-- Begin static table -->
<h4 class="small-title" style="margin-top: 30px">PROFESSEURS</h4>

<div class="the-box">
	<div class="table-responsive">
		<table class="table table-striped table-hover datatable" id="teachers-datatable">
			<thead class="the-box dark full">
				<tr>
          <th style="width:200px">Actions</th>
          <th style="width:50px">ID</th>
          <th style="width:200px">Prénom</th>
          <th style="width:200px">Nom</th>
                  </tr>
			</thead>
			<tbody id="teachers_data">
        <% @teachers.each do |teacher| %>
          <tr id="teachers_<%= teacher.id %>">
            <td>
              <a class="btn btn-info" href="<%= edit_admin_teacher_path(teacher) %>"><i class="fa fa-pencil"></i> Modifier</a>
              <a class="btn btn-danger delete-record" data-id="<%= teacher.id %>" data-type="teachers"><i class="fa fa-trash-o"></i> Supprimer</a>
            </td>
            <td><%= teacher.id %></td>
            <td><%= teacher.firstname %></td>
            <td><%= teacher.lastname %></td>
            
          </tr>
        <% end %>
			</tbody>
		</table>
	</div><!-- /.table-responsive -->
</div><!-- /.the-box -->
<!-- End static table -->